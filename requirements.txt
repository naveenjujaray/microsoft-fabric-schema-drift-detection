anthropic>=0.39.0
azure-identity>=1.19.0
requests>=2.32.0
PyYAML>=6.0.2
duckdb>=1.1.0
rich>=13.9.0
python-dotenv>=1.0.1
tenacity>=9.0.0
pytest>=8.3.0
pytest-mock>=3.14.0

# Direct-connect source drivers are OPTIONAL extras - install per source:
#   pip install .[hana]        (hdbcli)
#   pip install .[snowflake]   (snowflake-connector-python)
#   pip install .[live]        (pyodbc, Fabric SQL endpoint)
